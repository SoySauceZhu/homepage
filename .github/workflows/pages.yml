name: Deploy site to GitHub Pages

on:
  push:
    branches: [ main ]   # change to 'master' if that's your default branch
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Sets up Ruby and caches gems; uses your Gemfile (works with academicpages)
      - name: Setup Ruby
        uses: ruby/setup-ruby@v1
        with:
          bundler-cache: true

      # Official Jekyll → Pages builder (handles BASEURL, etc.)
      - name: Build with Jekyll for Pages
        uses: actions/jekyll-build-pages@v1
        with:
          source: .          # where your site source is
          destination: _site # build output directory

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
